services:
  app:
    build: .
    volumes:
      - ./dataset:/app/dataset
      - ./src:/app/src
    environment:
      - PYTHONUNBUFFERED=1
      - APP_ENV=development
    command: python src/main.py
