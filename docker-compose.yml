services:
  app:
    build: .
    volumes:
      - ./dataset:/app/dataset
      - ./src:/app/src
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
      - APP_ENV=development
    command: python src/main.py
